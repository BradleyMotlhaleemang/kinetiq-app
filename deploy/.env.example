# Copy to deploy/.env on the server (or set in Coolify). Never commit deploy/.env.

DOMAIN=yourdomain.com
POSTGRES_PASSWORD=changeme-use-long-random-string

# GHCR images (set by CI; override for local builds)
# KINETIQ_API_IMAGE=ghcr.io/your-org/kinetiq-api:latest
# KINETIQ_APP_IMAGE=ghcr.io/your-org/kinetiq-app:latest

JWT_SECRET=
JWT_REFRESH_SECRET=

SMTP_USER=
SMTP_PASS=

SENTRY_DSN=
SENTRY_RELEASE=
